#include<stdio.h>#include "fact.h"//// int factorial ( int x ) {//   int f=1, i;//   for ( i = x ; i >= 1 ; i-- ) {//     f=f*i;//     // printf ("\nThe intermediate value of f is %d",f);//   }//   return f ;// }int rec ( int x ) {  int f;  if ( x == 1 )    return ( 1 ) ;  else    f = x * rec ( x - 1 ) ;  return f ;}int main( ) {  int a, fact1, fact2 ;  printf ( "\nEnter any number " ) ;  scanf ( "%d", &a ) ;  // fact1 = factorial ( a ) ;  fact2 = rec(a);  // printf ( "\nFactorial value = %d", fact1 ) ;  printf ( "\nFactorial value = %d", fact2 ) ;  int res = factorial(5);  printf ("\n %d",res);  return 0;}